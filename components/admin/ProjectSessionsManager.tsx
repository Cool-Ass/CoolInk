"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { useToast } from "@/components/admin/ToastProvider";
import { appointmentStatusLabel } from "@/lib/workflowStatus";
type Session={id:string;startsAt:Date;endsAt:Date;status:string;notes:string|null;price:number|null};
const labels:Record<string,string>={requested:appointmentStatusLabel("requested"),proposed:appointmentStatusLabel("proposed"),confirmed:appointmentStatusLabel("confirmed"),completed:appointmentStatusLabel("completed"),no_show:appointmentStatusLabel("no_show"),cancelled:appointmentStatusLabel("cancelled")};
const local=(d:Date)=>{const p=(n:number)=>String(n).padStart(2,"0");return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`};
export default function ProjectSessionsManager({sessions}:{sessions:Session[]}){const router=useRouter(),{showToast}=useToast(),[editing,setEditing]=useState<Session|null>(null),[busy,setBusy]=useState(false);async function save(){if(!editing)return;setBusy(true);try{const r=await fetch(`/api/admin/appointments/${editing.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(editing)}),j=await r.json();if(!r.ok)throw Error(j.error);showToast("Sesja została zapisana.");setEditing(null);router.refresh()}catch(e){showToast(e instanceof Error?e.message:"Nie udało się zapisać sesji.","error")}finally{setBusy(false)}}async function cancel(){if(!editing||!confirm("Anulować wizytę? Klient dostanie powiadomienie, a termin zostanie w historii."))return;setBusy(true);try{const r=await fetch(`/api/admin/appointments/${editing.id}`,{method:"DELETE"}),j=await r.json();if(!r.ok)throw Error(j.error);showToast("Wizyta została anulowana.");setEditing(null);router.refresh()}catch(e){showToast(e instanceof Error?e.message:"Nie udało się anulować wizyty.","error")}finally{setBusy(false)}}return <section className="border-t border-ink-white/10 pt-5"><p className="text-[11px] tracking-widest text-ink-gold">SESJE PROJEKTU</p><p className="mt-1 text-xs text-ink-grey">Kliknij sesję, aby zmienić termin, czas, cenę lub status.</p><div className="mt-3 space-y-2">{sessions.length?sessions.map(s=><button type="button" key={s.id} onClick={()=>setEditing(s)} className="flex w-full justify-between gap-3 border border-ink-white/10 px-3 py-3 text-left text-sm hover:border-ink-gold/60"><span>{s.startsAt.toLocaleString("pl-PL",{dateStyle:"medium",timeStyle:"short"})} · {Math.round((s.endsAt.getTime()-s.startsAt.getTime())/60000)} min{s.price!==null?` · ${s.price} zł`:""}</span><span className="text-ink-gold">{labels[s.status]??s.status}</span></button>):<p className="text-sm text-ink-grey">Brak zaplanowanych sesji.</p>}</div>{editing&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-ink-black/80 p-4"><div className="w-full max-w-xl border border-ink-white/20 bg-ink-charcoal p-6"><div className="flex justify-between"><div><p className="text-[11px] tracking-widest text-ink-gold">EDYCJA SESJI</p><h3 className="mt-1 font-display text-2xl">Wizyta klienta</h3></div><button onClick={()=>setEditing(null)}>✕</button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs text-ink-grey">OD<input type="datetime-local" step="1800" value={local(new Date(editing.startsAt))} onChange={e=>setEditing({...editing,startsAt:new Date(e.target.value)})} className="mt-2 w-full border border-ink-white/20 bg-transparent p-2 text-ink-white"/></label><label className="text-xs text-ink-grey">DO<input type="datetime-local" step="1800" value={local(new Date(editing.endsAt))} onChange={e=>setEditing({...editing,endsAt:new Date(e.target.value)})} className="mt-2 w-full border border-ink-white/20 bg-transparent p-2 text-ink-white"/></label><label className="text-xs text-ink-grey">STATUS<select value={editing.status} onChange={e=>setEditing({...editing,status:e.target.value})} className="mt-2 w-full border border-ink-white/20 bg-ink-black p-2 text-ink-white">{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="text-xs text-ink-grey">CENA (PLN)<input inputMode="numeric" value={editing.price??""} onChange={e=>setEditing({...editing,price:e.target.value===""?null:Number(e.target.value)})} className="mt-2 w-full border border-ink-white/20 bg-transparent p-2 text-ink-white"/></label></div><label className="mt-4 block text-xs text-ink-grey">NOTATKA<textarea value={editing.notes??""} onChange={e=>setEditing({...editing,notes:e.target.value})} rows={4} className="mt-2 w-full border border-ink-white/20 bg-transparent p-2 text-ink-white"/></label><div className="mt-5 flex flex-wrap gap-3"><button disabled={busy} onClick={save} className="border border-ink-gold px-4 py-2 text-xs text-ink-gold disabled:opacity-50">{busy?"ZAPISYWANIE…":"ZAPISZ"}</button><button disabled={busy||editing.status==="cancelled"} onClick={cancel} className="border border-red-400/60 px-4 py-2 text-xs text-red-200 disabled:opacity-50">ANULUJ WIZYTĘ</button><button onClick={()=>setEditing(null)} className="text-xs text-ink-grey">Wróć</button></div></div></div>}</section>}
