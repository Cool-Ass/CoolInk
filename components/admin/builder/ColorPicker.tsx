"use client";

const CREATIVE_PALETTE = ["#090807", "#111111", "#24211e", "#f5f1e8", "#ffffff", "#c99a4a", "#f0bc62", "#8d5a2b", "#7f1d1d", "#dc2626", "#fb7185", "#7c3aed", "#2563eb", "#06b6d4", "#059669", "#84cc16"];

export default function ColorPicker({ value = "", onChange, label = "KOLOR", palette = true }: { value?: string; onChange: (value: string) => void; label?: string; palette?: boolean }) {
  const safe = /^#[0-9a-f]{6}$/i.test(value) ? value : "#c99a4a";
  return <div className="flex min-w-0 flex-col gap-2 text-[10px] tracking-[0.08em] text-white/65"><span>{label}</span><span className="flex min-w-0 gap-2"><input aria-label={`${label} — próbnik`} type="color" value={safe} onChange={(e) => onChange(e.target.value)} className="h-11 w-12 shrink-0 cursor-pointer border border-white/20 bg-transparent p-1 hover:border-white/40" /><input aria-label={`${label} — wartość`} value={value} onChange={(e) => onChange(e.target.value)} placeholder="#C99A4A lub transparent" className="h-11 min-w-0 flex-1 border border-white/20 bg-[#17191c] px-2.5 text-[12px] uppercase text-white outline-none hover:border-white/35 focus-visible:border-ink-gold focus-visible:ring-1 focus-visible:ring-ink-gold" /></span>{palette && <span className="grid min-w-0 gap-1" style={{ gridTemplateColumns: "repeat(auto-fit, minmax(40px, 1fr))" }} role="group" aria-label="Dodatkowa paleta kolorów">{CREATIVE_PALETTE.map((color) => { const active = value.toLowerCase() === color; return <button key={color} type="button" title={color} aria-label={`Ustaw kolor ${color}`} aria-pressed={active} onClick={() => onChange(color)} className={`h-11 min-w-10 border transition-transform active:scale-95 ${active ? "border-white ring-2 ring-ink-gold" : "border-white/20 hover:border-white/60"}`} style={{ backgroundColor: color }} />; })}</span>}</div>;
}
