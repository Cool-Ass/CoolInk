"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { useToast } from "@/components/admin/ToastProvider";
import { ADMIN_STATUS_LABEL, DEPOSIT_STATUS } from "@/lib/projectWorkflow";

const OPTIONS = Object.entries(ADMIN_STATUS_LABEL);

export default function ProjectManager({ id, initialStatus, initialNotes, estimatedPrice, finalPrice, initialDepositStatus, depositAmount, depositPaymentMethod }: { id: string; initialStatus: string; initialNotes: string | null; estimatedPrice: number | null; finalPrice: number | null; initialDepositStatus: string; depositAmount: number | null; depositPaymentMethod: string | null }) {
 const router = useRouter(); const { showToast } = useToast(); const [status, setStatus] = useState(initialStatus); const [notes, setNotes] = useState(initialNotes ?? ""); const [estimate, setEstimate] = useState(estimatedPrice?.toString() ?? ""); const [final, setFinal] = useState(finalPrice?.toString() ?? ""); const [depositStatus, setDepositStatus] = useState(initialDepositStatus); const [deposit, setDeposit] = useState(depositAmount?.toString() ?? ""); const [depositMethod, setDepositMethod] = useState(depositPaymentMethod ?? ""); const [saving, setSaving] = useState(false); const [proposalOpen, setProposalOpen] = useState(false); const [proposal, setProposal] = useState({ startsAt: "", duration: "60", note: "" }); const [proposing, setProposing] = useState(false);
 async function save() { setSaving(true); try { const res = await fetch(`/api/admin/projects/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status, internalNotes: notes, estimatedPrice: estimate, finalPrice: final, depositStatus, depositAmount: deposit, depositPaymentMethod: depositMethod }) }); const data = await res.json(); if (!res.ok) throw new Error(data.error); showToast("Projekt zaktualizowany."); router.refresh(); } catch (e) { showToast(e instanceof Error ? e.message : "Nie udało się zapisać.", "error"); } finally { setSaving(false); } }
 async function propose(event: FormEvent) { event.preventDefault(); const startsAt = new Date(proposal.startsAt); const endsAt = new Date(startsAt); endsAt.setMinutes(endsAt.getMinutes() + Number(proposal.duration)); setProposing(true); try { const res = await fetch(`/api/admin/projects/${id}/proposed-appointment`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ startsAt, endsAt, note: proposal.note }) }); const data = await res.json(); if (!res.ok) throw new Error(data.error); showToast("Propozycja terminu jest widoczna na koncie klienta."); setProposalOpen(false); setProposal({ startsAt: "", duration: "60", note: "" }); router.refresh(); } catch (error) { showToast(error instanceof Error ? error.message : "Nie udało się zaproponować terminu.", "error"); } finally { setProposing(false); } }
 return <section className="border border-ink-white/15 bg-ink-charcoal/40 p-5"><p className="text-[11px] tracking-[0.14em] text-ink-gold">ZARZĄDZANIE PROJEKTEM</p><div className="mt-5 grid gap-4 sm:grid-cols-3"><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">STATUS<select value={status} onChange={(e) => setStatus(e.target.value)} className="border border-ink-white/20 bg-ink-black px-3 py-2.5 text-sm normal-case tracking-normal text-ink-white outline-none focus:border-ink-gold">{OPTIONS.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">WYCENA (PLN)<input inputMode="numeric" value={estimate} onChange={(e) => setEstimate(e.target.value)} className="border border-ink-white/20 bg-transparent px-3 py-2.5 text-sm normal-case tracking-normal text-ink-white outline-none focus:border-ink-gold" /></label><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">CENA KOŃCOWA (PLN)<input inputMode="numeric" value={final} onChange={(e) => setFinal(e.target.value)} className="border border-ink-white/20 bg-transparent px-3 py-2.5 text-sm normal-case tracking-normal text-ink-white outline-none focus:border-ink-gold" /></label></div><div className="mt-4 grid gap-4 border-t border-ink-white/10 pt-4 sm:grid-cols-3"><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">ZADATEK<select value={depositStatus} onChange={(e) => setDepositStatus(e.target.value)} className="border border-ink-white/20 bg-ink-black px-3 py-2.5 text-sm text-ink-white">{DEPOSIT_STATUS.map((value) => <option key={value} value={value}>{({ not_required: "Niewymagany", awaiting: "Oczekuje", paid: "Opłacony", refunded: "Zwrócony", forfeited: "Utracony" } as Record<string, string>)[value]}</option>)}</select></label><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">KWOTA ZADATKU (PLN)<input inputMode="numeric" value={deposit} onChange={(e) => setDeposit(e.target.value)} className="border border-ink-white/20 bg-transparent px-3 py-2.5 text-sm text-ink-white" /></label><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">METODA PŁATNOŚCI<input value={depositMethod} onChange={(e) => setDepositMethod(e.target.value)} placeholder="np. przelew, gotówka" className="border border-ink-white/20 bg-transparent px-3 py-2.5 text-sm text-ink-white" /></label></div><label className="mt-4 flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">NOTATKI WEWNĘTRZNE<textarea value={notes} onChange={(e) => setNotes(e.target.value)} rows={5} className="border border-ink-white/20 bg-transparent px-3 py-2.5 text-sm normal-case tracking-normal text-ink-white outline-none focus:border-ink-gold" /></label><div className="mt-5 flex flex-wrap gap-3"><button type="button" onClick={save} disabled={saving} className="border border-ink-gold px-4 py-2.5 text-xs tracking-[0.08em] text-ink-gold hover:bg-ink-gold hover:text-ink-black disabled:opacity-50">{saving ? "ZAPISYWANIE…" : "ZAPISZ ZMIANY"}</button><button type="button" onClick={() => setProposalOpen((value) => !value)} className="border border-emerald-500/50 px-4 py-2.5 text-xs tracking-[0.08em] text-emerald-300 hover:bg-emerald-500/10">ZAPROPONUJ TERMIN</button></div>{proposalOpen && <form onSubmit={propose} className="mt-5 border border-emerald-500/25 bg-emerald-500/5 p-4"><p className="text-xs text-emerald-200">Klient zobaczy proponowany termin na swoim koncie.</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">DATA I GODZINA<input required type="datetime-local" step="1800" value={proposal.startsAt} onChange={(event) => setProposal({ ...proposal, startsAt: event.target.value })} className="border border-ink-white/20 bg-ink-black px-3 py-2.5 text-sm normal-case tracking-normal text-ink-white" /></label><label className="flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">CZAS TRWANIA<select value={proposal.duration} onChange={(event) => setProposal({ ...proposal, duration: event.target.value })} className="border border-ink-white/20 bg-ink-black px-3 py-2.5 text-sm text-ink-white">{[30,60,90,120,180,240,300,360,480,600,720].map((minutes) => <option key={minutes} value={minutes}>{minutes < 60 ? "30 min" : `${minutes / 60} h`}</option>)}</select></label></div><label className="mt-4 flex flex-col gap-2 text-[11px] tracking-[0.1em] text-ink-grey">WIADOMOŚĆ (OPCJONALNIE)<textarea value={proposal.note} onChange={(event) => setProposal({ ...proposal, note: event.target.value })} rows={2} className="border border-ink-white/20 bg-transparent px-3 py-2.5 text-sm normal-case tracking-normal text-ink-white" /></label><div className="mt-4 flex gap-3"><button disabled={proposing} className="border border-emerald-400 px-4 py-2 text-xs text-emerald-200 disabled:opacity-50">{proposing ? "WYSYŁANIE…" : "WYŚLIJ PROPOZYCJĘ"}</button><button type="button" onClick={() => setProposalOpen(false)} className="text-xs text-ink-grey">Anuluj</button></div></form>}</section>;
}
