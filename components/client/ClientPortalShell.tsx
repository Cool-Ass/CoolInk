"use client";
import Link from "next/link";
import Image from "next/image";
import { CalendarCheck, FileText, Home, Menu, User } from "lucide-react";
import { usePathname } from "next/navigation";
import { useState, type ReactNode } from "react";
import ClientHeaderUtilities from "@/components/client/ClientHeaderUtilities";
import ClientLogoutButton from "@/components/client/ClientLogoutButton";
const links = [["/app/portal", "START", Home, true], ["/app/portal/projects", "PROJEKTY", FileText], ["/app/portal/calendar", "KALENDARZ", CalendarCheck], ["/app/portal/documents", "DOKUMENTY", FileText], ["/app/portal/profile", "PROFIL", User]] as const;
const primaryLinks = links.slice(0, 4), secondaryLinks = links.slice(4);
export default function ClientPortalShell({ firstName, unreadMessages, unreadNotifications, messages, notifications, children }: { firstName: string; unreadMessages: number; unreadNotifications: number; messages: { id: string; projectId: string; project: string; body: string; createdAt: string; unread: boolean }[]; notifications: { id: string; title: string; body: string; href: string | null; createdAt: string; unread: boolean }[]; children: ReactNode }) { const pathname = usePathname(), [moreOpen, setMoreOpen] = useState(false), [logoFailed, setLogoFailed] = useState(false); const active = (href: string, exact?: boolean) => exact ? pathname === href : pathname.startsWith(href); return <main className="min-h-screen bg-ink-black px-4 py-5 text-ink-white sm:px-6 lg:px-10 lg:py-10"><header className="mx-auto flex max-w-7xl items-center justify-between border-b border-ink-white/15 pb-4"><Link href="/" aria-label="CoolInk Tattoo Studio" className="flex h-10 w-32 items-center">{logoFailed ? <span className="font-display text-xl tracking-wide text-ink-white">COOLINK</span> : <Image src="/images/logo-white.jpg" alt="CoolInk Tattoo Studio" width={128} height={40} priority onError={() => setLogoFailed(true)} className="h-full w-full object-contain object-left" />}</Link><div className="flex items-center gap-2"><ClientHeaderUtilities messages={messages} notifications={notifications} unreadMessages={unreadMessages} unreadNotifications={unreadNotifications} /><ClientLogoutButton /></div></header><div className="mx-auto grid max-w-7xl gap-7 py-7 lg:grid-cols-[230px_minmax(0,1fr)]"><aside className="hidden h-fit border border-ink-white/15 bg-ink-charcoal/30 p-4 lg:sticky lg:top-6 lg:block"><p className="text-[10px] tracking-[.16em] text-ink-gold">TWOJE KONTO</p><p className="mt-2 font-display text-2xl">{firstName}</p><nav className="mt-4 border-y border-ink-white/10 py-3 text-xs">{links.map(([href, label, Icon, exact]) => <Link key={href} href={href} className={`flex items-center gap-3 px-2 py-3 ${active(href, exact) ? "text-ink-gold" : "text-ink-grey hover:text-ink-gold"}`}><Icon className="h-4 w-4" />{label}</Link>)}</nav></aside><div className="min-w-0 pb-20 lg:pb-0">{children}</div></div><nav aria-label="Główna nawigacja klienta" className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-5 border-t border-ink-white/15 bg-ink-charcoal px-1 py-2 lg:hidden">{primaryLinks.map(([href, label, Icon, exact]) => <Link key={href} href={href} onClick={() => setMoreOpen(false)} className={`flex min-h-12 flex-col items-center justify-center gap-1 px-1 text-[9px] ${active(href, exact) ? "text-ink-gold" : "text-ink-grey"}`}><Icon className="h-4 w-4" />{label}</Link>)}<button type="button" onClick={() => setMoreOpen((open) => !open)} aria-expanded={moreOpen} className="flex min-h-12 flex-col items-center justify-center gap-1 px-1 text-[9px] text-ink-grey"><Menu className="h-4 w-4" />WIĘCEJ</button></nav>{moreOpen && <div className="fixed inset-x-3 bottom-[76px] z-40 border border-ink-white/15 bg-ink-charcoal p-3 shadow-2xl lg:hidden"><div className="grid grid-cols-1 gap-2">{secondaryLinks.map(([href, label, Icon, exact]) => <Link key={href} href={href} onClick={() => setMoreOpen(false)} className={`flex min-h-16 items-center justify-center gap-2 border text-[10px] ${active(href, exact) ? "border-ink-gold text-ink-gold" : "border-ink-white/15 text-ink-grey"}`}><Icon className="h-4 w-4" />{label}</Link>)}</div></div>}</main>; }
