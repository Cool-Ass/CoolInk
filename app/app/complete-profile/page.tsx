"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function Complete(){const r=useRouter(),[v,setV]=useState({firstName:"",lastName:"",phone:""}),[e,setE]=useState("");const save=async()=>{const x=await fetch("/api/client/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)}),j=await x.json();if(!x.ok)return setE(j.error);r.push("/app/portal")};return <main className="flex min-h-screen items-center justify-center bg-ink-black p-6 text-ink-white"><section className="w-full max-w-md border border-ink-white/15 p-7"><p className="text-xs tracking-widest text-ink-gold">JESZCZE CHWILA</p><h1 className="mt-3 font-display text-3xl">Uzupełnij dane.</h1><p className="mt-2 text-sm text-ink-grey">Są potrzebne do obsługi wizyt i zgłoszeń.</p><div className="mt-6 space-y-3">{(["firstName","lastName","phone"]as const).map(k=><input key={k} value={v[k]} onChange={x=>setV({...v,[k]:x.target.value})} placeholder={{firstName:"Imię",lastName:"Nazwisko",phone:"Telefon (opcjonalnie)"}[k]} className="w-full border border-ink-white/20 bg-transparent p-3"/>)}<button onClick={save} className="w-full border border-ink-gold bg-ink-gold p-3 text-xs text-ink-black">ZAPISZ I PRZEJDŹ DALEJ</button>{e&&<p className="text-sm text-red-300">{e}</p>}</div></section></main>}
