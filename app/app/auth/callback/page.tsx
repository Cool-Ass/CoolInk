"use client";import {useEffect,useState}from"react";import{useRouter}from"next/navigation";export default function Callback(){const r=useRouter(),[m,setM]=useState("Kończę bezpieczne logowanie…");useEffect(()=>{const p=new URLSearchParams(window.location.hash.slice(1));fetch("/api/client/auth/oauth-session",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({access_token:p.get("access_token"),refresh_token:p.get("refresh_token")})}).then(x=>x.ok?r.push("/app/complete-profile"):setM("Nie udało się zalogować. Spróbuj ponownie.")).catch(()=>setM("Nie udało się zalogować."))},[r]);return <main className="flex min-h-screen items-center justify-center bg-ink-black text-ink-white"><p>{m}</p></main>}
