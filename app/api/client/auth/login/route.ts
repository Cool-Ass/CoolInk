import { NextResponse } from "next/server";
import { clientCookieOptions, CLIENT_ACCESS_COOKIE, CLIENT_REFRESH_COOKIE, supabaseAuth } from "@/lib/clientAuth";
export async function POST(request: Request) { const body = await request.json().catch(() => null); const email = String(body?.email ?? "").trim().toLowerCase(); const password = String(body?.password ?? ""); const result = await supabaseAuth("/token?grant_type=password", { method: "POST", body: JSON.stringify({ email, password }) }); const data = await result.json(); if (!result.ok) return NextResponse.json({ error: data.error_description ?? "Nieprawidłowy e-mail lub hasło." }, { status: result.status }); const response = NextResponse.json({ ok: true }); response.cookies.set(CLIENT_ACCESS_COOKIE, data.access_token, clientCookieOptions); response.cookies.set(CLIENT_REFRESH_COOKIE, data.refresh_token, clientCookieOptions); return response; }
