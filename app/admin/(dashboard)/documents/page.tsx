import { prisma } from "@/lib/prisma";
import NewDocumentForm from "@/components/admin/NewDocumentForm";
export const dynamic = "force-dynamic";
const CATEGORY: Record<string, string> = { consent: "ZGODA", preparation: "PRZYGOTOWANIE", aftercare: "PIELĘGNACJA", policy: "REGULAMIN", other: "INNE" };
export default async function DocumentsPage() { const documents = await prisma.studioDocument.findMany({ orderBy: { updatedAt: "desc" } }); return <div className="flex flex-col gap-8"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-[11px] tracking-[0.16em] text-ink-gold">TREŚCI OPERACYJNE</p><h1 className="mt-2 font-display text-3xl text-ink-white">Dokumenty i zgody</h1><p className="mt-2 max-w-xl text-sm text-ink-grey">Wersjonowane dokumenty przygotowane pod cyfrowe akceptacje klienta.</p></div><NewDocumentForm /></div><div className="divide-y divide-ink-white/10 border border-ink-white/10">{documents.length === 0 ? <p className="p-10 text-center text-sm text-ink-grey">Nie ma jeszcze dokumentów.</p> : documents.map((document) => <article key={document.id} className="flex flex-col gap-2 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-base text-ink-white">{document.title}</p><p className="mt-1 text-[11px] tracking-[0.08em] text-ink-gold">{CATEGORY[document.category]} · WERSJA {document.version}</p></div><span className={`text-xs ${document.published ? "text-ink-gold" : "text-ink-grey"}`}>{document.published ? "OPUBLIKOWANY" : "SZKIC"}</span></article>)}</div></div>; }
